const prefix = 'auth-cognito';

export const AUTHENTICATE = `${prefix}/AUTHENTICATE`;
export const SIGNED_UP = `${prefix}/SIGNED_UP`;
export const SIGN_OUT = `${prefix}/SIGN_OUT`;
export const LOADING = `${prefix}/LOADING`;
export const ERROR = `${prefix}/ERROR`;
